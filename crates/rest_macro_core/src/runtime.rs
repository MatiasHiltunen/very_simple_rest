//! Compatibility facade for runtime settings and Actix compression middleware.

pub use vsr_runtime::runtime::actix::compression_middleware;
pub use vsr_runtime::runtime::{CompressionConfig, RuntimeConfig};
